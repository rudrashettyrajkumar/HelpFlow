# HelpFlow brain (FastAPI) runtime + tooling. Featherweight container stays the
# rule — no torch, no sentence-transformers; chat/embedding models are external
# APIs (ARCHITECTURE §2).
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic-settings==2.14.2
httpx>=0.28.1,<1          # google-genai (via langchain-google-genai) requires >=0.28.1
qdrant-client==1.18.0     # async vector client (§5.1)
asyncpg==0.31.0           # Supabase Postgres: guarded UPDATEs + LISTEN/NOTIFY (§5.2/§3.3)
python-jose[cryptography]==3.3.0  # HS256 JWT minting/verification (E5 auth, §7.1)

# Model layer v2 (E4): LangChain factory (5 BYOK providers) + LangGraph support
# graph — replaces the old Router-based gateway entirely (ARCHITECTURE §4.6, §13).
langchain-core~=1.4
langchain-openai~=1.3     # ChatOpenAI covers OpenRouter/Groq/OpenAI via base_url
langchain-anthropic~=1.4
langchain-google-genai~=4.2
langgraph>=1.0,<2
flashrank~=0.2            # open-source cross-encoder reranker (tiny ONNX, CPU)

# Ingestion (E2): chunk token-counting + website content extraction.
tiktoken==0.13.0          # cl100k_base token counting for the chunker (§3.1 STEP 3)
trafilatura==2.0.0        # main-content extraction from crawled HTML (§3.1 STEP 2)
beautifulsoup4==4.15.0    # link discovery in the BFS crawler (crawler.py, not extraction)

# Dev / CI (lint + test).
pytest==8.4.2
pytest-asyncio==0.25.3
ruff==0.15.20
